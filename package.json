{
  "name": "mindfulcloud",
  "version": "5.3.1",
  "private": true,
  "description": "A lightweight browser extension for calmer YouTube, YouTube Music, Reddit, Twitter/X, and Pinterest pages.",
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check .",
    "test": "node --test"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
