{
  "name": "mindfulcloud",
  "version": "4.7.0",
  "private": true,
  "description": "A lightweight browser extension for calmer YouTube, Reddit, Twitter/X, and Pinterest pages.",
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
